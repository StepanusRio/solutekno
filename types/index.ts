export type portofolioItem = {
  title: string;
  category: string;
  image: string;
  tech: string[];
};

export type testimonial = {
  name: string;
  company: string;
  text: string;
  avatar: string;
  rating: number;
};

export type blogPost = {
  title: string;
  excerpt: string;
  image: string;
  date: string;
  readTime: string;
};
